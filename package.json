{
  "name": "kipukas-pwa",
  "version": "1.0.0",
  "description": "Kipukas PWA with Workbox caching",
  "scripts": {
    "build:sw": "workbox injectManifest workbox-config.js",
    "build": "jekyll build && npm run build:sw"
  },
  "dependencies": {
    "workbox-cli": "^7.3.0",
    "tmp": ">=0.2.4"
  }
}
